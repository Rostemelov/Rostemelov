def randpassgen():
    import random
    l=[8,9,10,11,12,13]
    paslen=random.choice(l)
    i=0
    password=""
    while(i<paslen):
        pasci=random.choice(range(20,126))
        password=password+chr(pasci)
        i=i+1
    return(password)
randpassgen()
